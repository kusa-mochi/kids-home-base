# タイムゾーンの取り扱い

## フロントエンドでユーザーが入力する日時

ローカルタイムゾーンで解釈する。

## フロントエンドがバックエンドのAPIに渡す日時

UTCで渡す。

## DBに記録する日時

UTCで記録する。

## DBの検索に使う日時

ローカルタイムゾーンで検索する。

例えば、ローカルタイムゾーンが Asia/Tokyo のユーザーが「今日の計画」をバックエンドから取得する場合、ローカルのタイムゾーンにおける「今日」の計画をフロントエンドはバックエンドから取得する。

## 画面表示に使う日時

ローカルタイムゾーンで表示する。
